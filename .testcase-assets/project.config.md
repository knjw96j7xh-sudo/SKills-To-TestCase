# 项目配置文件 · project.config.md

> 说明：本文件在每次触发 /testcase-creator 时会被自动读取，用于减少重复告知 LLM 的上下文信息。
> 请根据实际项目情况填写，首次使用前必须完成配置。

---

## 基本信息

| 配置项 | 值 |
|--------|----|
| 项目名称 | [填写项目中文名，如：碳智慧平台] |
| 项目英文标识 | [填写英文缩写，如：carbonpy，用于文件命名] |
| 当前版本 | [如：v2.3.0] |
| 测试负责人 | [填写姓名] |
| 更新日期 | [YYYY-MM-DD] |

---

## 业务域列表

> LLM 在生成用例和选择检查点时，将优先从以下业务域中匹配分类。

| 域标识 | 中文名称 | 说明 |
|--------|----------|------|
| UC | 用户中心 | 注册、登录、找回密码、账号设置 |
| PAY | 支付 | 充值、提现、订单支付、退款 |
| LIST | 数据列表 | 通用列表、分页、搜索、筛选 |
| FILE | 文件管理 | 上传、下载、预览、删除 |
| RISK | 通用风险 | 并发、网络异常、权限越界 |
| API | 接口层 | 参数校验、鉴权、幂等性 |

> [!NOTE]
> 新增业务域时，同步在 checkpoints-index.md 中新增对应分类，并在此表追加一行。

---

## 常用导出路径

| 用途 | 路径 |
|------|------|
| 用例定稿存放目录 | `.testcase-assets/history/` |
| OTP 导入文件存放目录 | `.testcase-assets/history/` |
| OTP 系统导入入口 | [填写 OTP 系统 URL 或操作路径] |
| 用例评审文档共享目录 | [填写团队共享路径，如：飞书/乐享链接] |

---

## OTP 字段映射配置

> [!IMPORTANT]
> 这是最容易导致导入失败的环节。请提前与 OTP 系统管理员确认以下字段名称，
> 并同步更新 `.testcase-assets/templates/otp-schema.json`。

| 本方案字段名 | OTP 系统实际字段名 | 是否已对齐 |
|-------------|------------------|------------|
| `project` | [填写 OTP 字段名] | [ ] |
| `moduleName` | [填写 OTP 字段名] | [ ] |
| `suiteName` | [填写 OTP 字段名] | [ ] |
| `caseId` | [填写 OTP 字段名] | [ ] |
| `title` | [填写 OTP 字段名] | [ ] |
| `priority` | [填写 OTP 字段名，P0/P1/P2 or 高/中/低] | [ ] |
| `preconditions` | [填写 OTP 字段名] | [ ] |
| `steps[].action` | [填写 OTP 字段名] | [ ] |
| `steps[].expected` | [填写 OTP 字段名] | [ ] |
| `tags` | [填写 OTP 字段名，或填「不支持」] | [ ] |

> 全部勾选后，在 otp-schema.json 中统一替换字段名，再进行首次导入测试。

---

## 默认行为配置

| 配置项 | 当前值 | 说明 |
|--------|--------|------|
| 用例ID前缀 | TC | 生成用例时的编号前缀 |
| 默认优先级规则 | P0=异常场景（阻断性错误），P1=正向主流程/边界，P2=并发，P3=体验类 | LLM 分配优先级的参考 |
| 历史文件是否 Git 追踪 | 否（已加入 .gitignore） | 改为「是」则从 .gitignore 中删除对应规则 |
| 评审默认应用维度 | COMP-01,COMP-02,COMP-03,EXEC-01 | 每次评审时默认勾选的评审点 |
