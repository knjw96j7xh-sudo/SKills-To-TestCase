# 测试用例生成指南 · TESTCASE_GUIDE.md

> 适用工具：ChatGPT、GitHub Copilot Chat、Codex CLI 等无原生文件读写的纯对话工具。
> 使用方式：将本文件全文复制，连同检查点/评审点索引内容一起粘贴到对话开头，AI 将按流程执行。

---

## 使用前准备

在开始对话前，请将以下内容一并复制粘贴给 AI：

1. 本文件全文
2. `.testcase-assets/project.config.md` 全文
3. `.testcase-assets/checkpoints-index.md` 全文
4. `.testcase-assets/review-expectations-index.md` 全文

粘贴格式建议：

```
=== 测试用例生成指南 ===
[粘贴本文件内容]

=== 项目配置 ===
[粘贴 project.config.md 内容]

=== 检查点索引 ===
[粘贴 checkpoints-index.md 内容]

=== 评审点索引 ===
[粘贴 review-expectations-index.md 内容]

=== 需求内容 ===
[粘贴需求描述或接口文档]
```

> **需求来源类型**：支持以下 8 种，请在对话开头说明使用了哪种：
> - **A** 直接粘贴文字需求描述
> - **B** 乐享页面链接
> - **C** 接口文档链接或本地文件路径
> - **D** 本地技术方案文件（.md / .docx / .pdf）
> - **E** 图片/截图（.png / .jpg / .jpeg / .gif / .webp）— UI 设计稿、原型图、流程图
> - **F** 飞书文档链接（feishu.cn / larksuite.com）
> - **G** Excel 需求列表（.xlsx / .xls）
> - **H** 需求管理工具链接（Jira / Tapd / 禅道）
>
> 纯对话工具无法自动读取链接和文件，请将内容手动粘贴到对话中。图片可通过上传附件方式提供。

---

## 核心原则

- 先确认输入，不自动导入，不生成自动化测试代码
- 每个阶段结束后等待用户确认，再进入下一阶段
- 检查点和评审点可随项目经验持续沉淀

---

## 五阶段执行流程

### 阶段 1：需求输入

收到对话后（参考上述 8 种需求来源类型），提取以下信息并输出确认清单：

```
[需求解析结果 - 请确认]

- 测试对象：[功能/模块名称]
- 业务规则：
  1. [规则1]
  2. [规则2]
- 限制条件：[如：仅限已登录用户]
- 接口/端点：[如有]
- 涉及角色：[如：普通用户、管理员]

若解析正确，请回复「确认」继续阶段 2。
若需修正，请指出错误内容。
```

---

### 阶段 2：检查点选择

展示已粘贴的检查点索引，按分类列出，询问用户选择：

```
[阶段 2 - 检查点选择]
请选择适用于本次需求的检查点编号（多个用逗号分隔）：

>> 业务域：用户中心（UC）
  [UC-01] 手机号格式校验
  [UC-02] 验证码发送间隔限制
  ...

>> 通用风险（RISK）
  [RISK-01] 并发场景数据一致性
  ...

请输入编号（如：UC-01,RISK-02），或输入「全选」/「跳过」
```

收到选择后，生成结构化摘要：

```
[输入结构化结果 - 0-用例准备]

需求要素：
| 项目 | 内容 |
|------|------|
| 测试对象 | ... |
| 业务规则 | ... |
| 限制条件 | ... |

已关联检查点：
| 编号 | 描述 | 分类 |
|------|------|------|
| UC-01 | 手机号格式校验 | 用户中心 |

结构化完成，请回复「生成用例」进入阶段 3。
```

> [!NOTE]
> 手动将此内容保存为 `.testcase-assets/history/<YYYYMMDD>_<HHMMSS>_<模块名>/0-用例准备.md`

---

### 阶段 3：用例生成

用户回复「生成用例」后，输出完整用例表：

```
[测试用例表 - 初稿]

| 用例ID | 测试点 | 前置条件 | 操作步骤 | 预期结果 | 关联检查点 | 场景类型 | 优先级 |
|--------|--------|----------|----------|----------|------------|----------|--------|
| TC-001 | ...    | ...      | 1. ...   | ...      | UC-01      | 正向     | P1     |
| TC-002 | ...    | ...      | 1. ...   | ...      | UC-01      | 异常     | P0     |
| TC-003 | ...    | ...      | 1. ...   | ...      | RISK-01    | 边界     | P1     |
| TC-004 | ...    | ...      | 1. ...   | ...      | RISK-01    | 并发     | P2     |

共生成 X 条：正向 X / 异常 X / 边界 X / 并发 X

请回复「进入评审」进入阶段 4，或告诉我需要修改的内容。
```

> [!NOTE]
> 手动将此内容保存为 `.testcase-assets/history/<YYYYMMDD>_<HHMMSS>_<模块名>/1-评审记要.md`

---

### 阶段 4：评审优化（并行分维度 + 增量评审）

> 纯对话工具无法启动隔离 subagent，采用「角色扮演」模式模拟分维度评审：
> 按评审维度（UX/DATA/COMP/EXEC/BUG/SEC/PERF）逐维度审查，每个维度审查完成后输出该维度的评审报告，
> 最后合并所有维度结果。第 2 轮起自动启用增量评审，仅审查新增/修改用例。

用户回复「进入评审」后，展示评审点索引，让用户选择评审维度。

**评审范围**：
- 第 1 轮：全量用例参与评审
- 第 2 轮及之后（增量）：已有用例仅展示 ID+测试点摘要，新增/修改用例展示完整信息

**逐维度审查**（按分类逐个执行）：

对每个选中的维度，执行以下 prompt：

```
请以独立测试评审专家的身份，专注于「{维度名称}」维度，对以下用例表进行审查。

## 需求要素
[粘贴需求摘要]

## 评审维度：{维度名称}
[仅粘贴该维度的评审点编号和描述]

## 待评审用例表
[粘贴用例表 — 第 1 轮为全量，后续轮次为增量]

请输出：
1. 评审报告表格（评审点 / 是否已覆盖 / 分析说明 / 建议补充用例）
2. 建议补充用例表（如有，需包含完整的用例行信息）
3. {维度名称} 维度评审结论
```

**合并结论**：

```
[分维度评审合并结论]

| 维度 | 评审点 | 评审结论 | 处理方式 |
|------|--------|----------|----------|
| UX   | UX-01  | [OK]     | 已覆盖 |
| BUG  | BUG-01 | [FAIL]   | 未覆盖，补充 TC-005 |
| COMP | COMP-02| [WARN]   | 需关注 |

[建议补充用例]（按维度去重）
| 用例ID | 测试点 | 来源维度 |
|--------|--------|----------|
| TC-005 | ...    | BUG |
| TC-006 | ...    | COMP |

请选择操作：
  A. 接受所有补充（合并后继续评审）
  B. 仅接受共识项（忽略待确认项）
  C. 部分接受（指定编号）
  D. 手动修改
  E. 忽略，进入阶段 5
```

**文件输出**：
- 评审报告写入 `1-评审报告-第N轮.md`（N 为当前轮次）
- `1-评审记要.md` 仅保留最终用例表（不含评审记录）

支持多轮迭代，直到用户满意后进入阶段 5。

---

### 阶段 5：定稿导出

输出完整最终用例表，询问导出平台（可多选）：

```
[阶段 5 - 定稿导出]
用例定稿已完成，共 X 条。

请选择导出平台（可多选，逗号分隔）：
  J. Jira CSV（.csv，可直接导入 Jira）
  E. Excel（.xlsx 表格，带颜色分类、冻结表头）
  X. XMind（思维导图，用于用例展示和评审）
  N. 不需要导出，本次到此结束

> 注意：Excel/XMind 导出需要在有 Python + openpyxl 环境的机器上运行导出脚本，
> 纯对话工具只能生成中间 JSON 数据，由您手动执行脚本完成转换。
> Jira CSV 可直接由 AI 生成。

[流程结束摘要]
| 项目 | 详情 |
|------|------|
| 测试对象 | ... |
| 用例总数 | X 条（正向X / 异常X / 边界X / 并发X） |
| 关联检查点 | X 个 |
| 评审轮次 | X 轮 |
| 运行目录 | .testcase-assets/history/<运行目录>/ |

是否有新的检查点或评审点需要沉淀？回复「沉淀」继续。
```

> [!NOTE]
> 手动将定稿内容保存为 `.testcase-assets/history/<YYYYMMDD>_<HHMMSS>_<模块名>/2-用例定稿.md`
> 若生成了 Jira CSV，保存为同目录下的 `jira_export.csv`
> Excel/XMind 导出脚本运行命令：
>   python3 .testcase-assets/scripts/md_to_csv.py <运行目录>/2-用例定稿.md <运行目录>/jira_export.csv
>   python3 .testcase-assets/scripts/export_excel.py <运行目录>/export_data.json <运行目录>/testcases.xlsx
>   python3 .testcase-assets/scripts/export_xmind.py <运行目录>/export_data.json <运行目录>/testcases.xmind

---

### 资产沉淀（任意阶段可触发）

用户输入「沉淀」后，收集新检查点/评审点信息，输出追加内容：

```
[新增检查点]
- [XX-XX] 描述内容  <!-- 新增于 YYYY-MM-DD，来源：[评审问题/用户补充/导入问题/漏测风险] -->

[新增评审点]
- [XX-XX] 描述内容  <!-- 新增于 YYYY-MM-DD，来源：... -->
```

> [!NOTE]
> 手动将上述内容追加到对应的 `.testcase-assets/checkpoints-index.md` 或
> `.testcase-assets/review-expectations-index.md` 文件末尾对应分类下。

---

## 快速触发话术

直接复制以下内容，粘贴到对话开头（附上资产内容后）：

### 完整流程（从头开始）

```
请按照「测试用例生成指南」的五阶段流程，帮我生成测试用例。
需求来源类型：[A/B/C/D/E/F/G/H]
需求内容如下：

[粘贴需求]
```

### 仅评审已有用例

```
请按照阶段 4 评审流程，对以下用例表进行独立视角评审。
请选择评审维度（如：UX,DATA,COMP,EXEC,BUG）。

[粘贴用例表]
```

### 仅导出定稿用例

```
请按照阶段 5 导出流程，将以下定稿用例导出为 [J/E/X] 格式。
项目名称：[填写项目名]
模块名称：[填写模块名]

[粘贴定稿用例表]
```

### 追问补充用例

```
请基于现有用例，针对以下检查点补充更多场景用例：
- [XX-XX] [检查点描述]

当前用例表：

[粘贴用例表]
```

### 资产沉淀

```
请按照资产沉淀流程，帮我新增以下内容：

新增检查点：
- [域标识] [描述]

新增评审点：
- [维度标识] [描述]
```

### 快速生成单模块用例

```
请帮我针对 [模块名] 生成测试用例，重点关注以下场景：
1. [场景1]
2. [场景2]
3. [场景3]

无需走完整流程，直接生成用例表即可。
```
